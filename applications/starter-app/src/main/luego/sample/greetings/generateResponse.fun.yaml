---
imports:
  - luego.text.*

exposable: true
output: 'sample.greetings.Response'
inputs:
  - name: the request
    type: 'sample.greetings.Request'
definition: >
  {
    let m = upper(the message of `the request`);
    Response(message = m);
  }
